<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.ednaldomartins.onemessenger">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.SEND_SMS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icone_one_messenger"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icone_one_messenger"
        android:supportsRtl="true"
        android:theme="@style/OneTheme">
        <activity android:name=".view.Activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.Activity.MainActivity"></activity>
    </application>

</manifest>